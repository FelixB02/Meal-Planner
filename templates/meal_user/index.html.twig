{% extends 'base.html.twig' %}

{% block title %}Meal index{% endblock %}

{% block body %}
    <h1>Meal index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Picture</th>
                <th>Category</th>
                <th>Calories</th>
                <th>Rating</th>
                <th>Preparation</th>
                <th>Cooking_time</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for meal in meals %}
            <tr>
                <td>{{ meal.id }}</td>
                <td>{{ meal.name }}</td>
                <td>{{ meal.picture }}</td>
                <td>{{ meal.category }}</td>
                <td>{{ meal.calories }}</td>
                <td>{{ meal.rating }}</td>
                <td>{{ meal.preparation }}</td>
                <td>{{ meal.cookingTime }}</td>
                <td>
                    <a href="{{ path('app_meal_user_show', {'id': meal.id}) }}">show</a>
                    <a href="{{ path('app_meal_user_edit', {'id': meal.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_meal_user_new') }}">Create new</a>
{% endblock %}
